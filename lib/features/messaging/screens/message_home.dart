import 'package:flutter_gram/utils/exporter.dart';

class MessagingHome extends StatelessWidget {
  /// Users Messaging Hub or home where all messages can be seen
  const MessagingHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
